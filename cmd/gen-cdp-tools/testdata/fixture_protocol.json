{
  "version": {"major": "1", "minor": "0"},
  "domains": [
    {
      "domain": "Test",
      "description": "Synthetic domain used only by gen-cdp-tools golden tests.",
      "types": [
        {
          "id": "Color",
          "type": "string",
          "enum": ["red", "green", "blue"]
        },
        {
          "id": "Width",
          "description": "Width in pixels.",
          "type": "integer"
        }
      ],
      "commands": [
        {
          "name": "ping",
          "description": "Health check.",
          "returns": [
            {"name": "ok", "type": "boolean"}
          ]
        },
        {
          "name": "draw",
          "description": "Draws a `pixel` on the canvas (the backtick is intentional — it stresses the schema-literal escaping).",
          "parameters": [
            {"name": "color", "$ref": "Color"},
            {"name": "width", "$ref": "Width", "optional": true},
            {"name": "label", "type": "string", "optional": true}
          ]
        }
      ]
    }
  ]
}
